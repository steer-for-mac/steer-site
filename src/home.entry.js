/* Homepage behaviour, one module per band. The theme script stays inline in
   <head> because it must set data-theme before first paint; the theme toggle
   lives in the layout, because all fourteen pages carry it. */

import "./scripts/launch-list.js";
import "./scripts/pad-labels.js";
import "./scripts/vignettes.js";
import "./scripts/gallery.js";
import "./scripts/plate-loops.js";
import "./scripts/couch-video.js";
