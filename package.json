{
  "name": "steer-site",
  "private": true,
  "type": "module",
  "description": "Marketing site for Steer. Static output, but the toolchain is real: see eleventy.config.js, bin/ci.",
  "scripts": {
    "build": "eleventy",
    "build:prod": "ELEVENTY_ENV=production eleventy",
    "ci": "bin/ci",
    "lint:css": "stylelint 'styles/*.css' 'parts/*.css'"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "html-validate": "^11.6.0",
    "lightningcss-cli": "^1.33.0",
    "purgecss": "^8.0.0",
    "stylelint": "^17.14.1",
    "stylelint-config-recommended": "^18.0.0"
  }
}
