{
  "name": "steer-site",
  "private": true,
  "type": "module",
  "description": "Marketing site for Steer. Static output, but the toolchain is real: see eleventy.config.js, scripts/ci.",
  "//": "Tasks are defined in the Makefile, once. The header there says why (JSON cannot carry the reasoning, and half the toolchain is Python). These two forward so `npm run build` and `npm run ci` work for anyone who reaches for npm first; they must never grow a command of their own. Everything else is `make help`.",
  "scripts": {
    "build": "make build",
    "ci": "make ci"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.1.2",
    "axe-core": "^4.12.1",
    "colorjs.io": "^0.7.1",
    "esbuild": "^0.28.1",
    "eslint": "^10.8.0",
    "globals": "^17.8.0",
    "html-minifier-next": "^7.5.2",
    "html-validate": "^11.6.0",
    "lightningcss-cli": "^1.33.0",
    "purgecss": "^8.0.0",
    "serve": "^14.2.6",
    "stylelint": "^17.14.1",
    "stylelint-config-recommended": "^18.0.0",
    "stylelint-declaration-strict-value": "^1.11.1",
    "typescript": "^7.0.2"
  }
}
